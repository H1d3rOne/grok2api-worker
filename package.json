{
  "name": "grok2api-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker edition of grok2api with OpenAI-compatible endpoints.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "dev:proxy": "HTTP_PROXY=http://127.0.0.1:10808 HTTPS_PROXY=http://127.0.0.1:10808 ALL_PROXY=http://127.0.0.1:10808 NO_PROXY=127.0.0.1,localhost wrangler dev",
    "setup:kv": "node scripts/setup-kv.mjs",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250601.0",
    "typescript": "^5.8.3",
    "wrangler": "^4.18.0"
  }
}
